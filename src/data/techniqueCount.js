/**
 * Hardcoded food count — avoids importing the entire foods barrel
 * into layout.tsx just for the metadata description string.
 * Update this when adding new foods.
 */
export const TECHNIQUE_COUNT = 18;
